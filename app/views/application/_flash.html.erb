<% if flash.any? %>
  <div class="flash">
    <% flash.each do |flash_type, flash_message| %>
      <div class="flash flash-<%= flash_type %>">
        <%= flash_message %>
      </div>
    <% end %>
  </div>
<% end %>
